<% flash.each do |type, message| %>
  <div class="alert <%= bootstrap_class_for(type) %> fade in whitebg" id="notice">
    <button class="close" data-dismiss="alert">×</button>
    <%= sanitize(message) %>
  </div>
<% end %>